# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Data Layer

## Current Goal

- Add the next planned feature unit here.

## Completed

- `01-design-system.md` — shadcn/ui installed (Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea), `lucide-react` installed, `lib/utils.ts` `cn()` helper added, dark-only theme tokens wired into `app/globals.css` and mapped to `context/ui-context.md` naming (`bg-base`, `text-copy-primary`, `border-surface-border`, `text-brand`, `bg-accent-dim`, etc.).
- `02-editor-chrome.md` — `components/editor/editor-navbar.tsx` (fixed-height top navbar, left/center/right sections, sidebar toggle with `PanelLeftOpen`/`PanelLeftClose`), `components/editor/project-sidebar.tsx` (floating slide-in sidebar with `isOpen` prop, `Projects` header + close button, `My Projects`/`Shared` tabs with empty placeholders, full-width `New Project` button). Dialog pattern already satisfied by existing shadcn `Dialog` (`components/ui/dialog.tsx`), which uses the project's color tokens and supports title/description/footer — no new dialog built. Both new components pass `tsc --noEmit` and `eslint` with no errors.
- `03-auth.md` — `proxy.ts` protects all routes except the sign-in/sign-up paths (read from `NEXT_PUBLIC_CLERK_SIGN_IN_URL`/`NEXT_PUBLIC_CLERK_SIGN_UP_URL`); `app/layout.tsx` wraps the app in `ClerkProvider` using `@clerk/ui/themes` `dark` theme with variables overridden to the project's CSS custom properties (no hardcoded colors); `app/sign-in` and `app/sign-up` use a shared `components/auth/auth-split-layout.tsx` two-panel layout (logo/tagline/feature list left, centered Clerk form right, form-only on small screens); `app/page.tsx` is now a server component redirecting authenticated users to `/editor` and unauthenticated users to `/sign-in`; `components/editor/editor-navbar.tsx` right section uses Clerk's `UserButton` directly (editor routes are already protected, so the signed-out branch was removed).
- `04-project-dialogs.md` — `lib/mock-projects.ts` (mock `Project[]` with `owner: "me" | "collaborator"`, `slugify()` helper); `hooks/use-project-dialogs.tsx` exports `ProjectDialogsProvider`/`useProjectDialogs()` context managing dialog type (`create`/`rename`/`delete`/`null`), active project, name/slug form state, and a mock `isLoading` (simulated 400ms delay, in-memory create/rename/delete — no API/persistence); `components/editor/project-dialogs.tsx` renders the three dialogs via the existing `components/ui/dialog.tsx`; `app/editor/page.tsx` is now the empty-state home (heading, description, `New Project` button, no cards) wired to `openCreateDialog`; `components/editor/project-sidebar.tsx` lists mock projects per tab, shows rename/delete icon-buttons only for `owner === "me"` items, and adds a `md:hidden` backdrop scrim that closes the sidebar on click/tap outside; `app/editor/layout.tsx` wraps navbar/sidebar/main in `ProjectDialogsProvider` and renders `<ProjectDialogs />`. `tsc --noEmit` and `eslint` both pass clean.
- `05-prisma.md` — `prisma/models/project.prisma` adds `Project` (`ownerId`, `name`, `description?`, `status: ProjectStatus` enum `DRAFT`/`ARCHIVED` default `DRAFT`, `canvasJsonPath?`, timestamps, `@@index([ownerId])`, `@@index([createdAt])`) and `ProjectCollaborator` (`projectId` with `onDelete: Cascade`, `email`, `createdAt`, `@@unique([projectId, email])`, `@@index([email])`, `@@index([projectId, createdAt])`); multi-file schema works via the existing `schema: "prisma/"` setting in `prisma.config.ts`. `lib/prisma.ts` is a cached singleton on `global` (dev-only) that branches on `DATABASE_URL`: `prisma+postgres://` uses `accelerateUrl` + `@prisma/extension-accelerate`, otherwise a direct `@prisma/adapter-pg` `PrismaPg` adapter. Installed missing runtime deps `@prisma/client`, `@prisma/adapter-pg`, `pg`, `@prisma/extension-accelerate` (spec said these were already installed; they weren't in `package.json`). Migration `prisma/migrations/20260727112612_init_project_models` applied and client generated to `app/generated/prisma` (already gitignored). `tsc --noEmit`, `eslint`, and `npm run build` all pass clean.
- `06-project-apis.md` — `app/api/projects/route.ts` (`GET` lists the authenticated user's projects via `prisma.project.findMany({ where: { ownerId: userId } })`; `POST` creates a project with `ownerId` from Clerk `auth()`, defaults `name` to `"Untitled Project"` when missing/blank, relies on the schema's `cuid()` default for `id`) and `app/api/projects/[projectId]/route.ts` (`PATCH` renames, `DELETE` removes; both look up the project first, return `403` if `project.ownerId !== userId`, `404` if the project doesn't exist). All four routes return `401` up front when `auth()` yields no `userId`. Backend-only — no UI wiring. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean; build output confirms `/api/projects` and `/api/projects/[projectId]` are registered as dynamic routes.
- `07-wire-editor-home.md` — replaced mock data end-to-end: deleted `lib/mock-projects.ts` and `hooks/use-project-dialogs.tsx`; `slugify()` moved to `lib/utils.ts`. Added `lib/projects.ts` (`getOwnedProjects(userId)`, `getSharedProjects(email)` — shared lookup is via `ProjectCollaborator.email`, matched against Clerk's `currentUser().primaryEmailAddress`, since the schema has no per-user collaborator ID). `app/editor/layout.tsx` is now an async server component: calls `auth()`/`currentUser()`, fetches both project lists server-side (no client fetch on initial load), and passes them into new client component `components/editor/editor-shell.tsx` (holds sidebar-open state + `ProjectActionsProvider`, renders navbar/sidebar/dialogs — this replaces the old client `editor/layout.tsx` body). `app/editor/page.tsx` is now a server component again; its `New Project` button was extracted into `components/editor/new-project-button.tsx` (the only client bit it needs). New hook `hooks/use-project-actions.tsx` (`ProjectActionsProvider`/`useProjectActions`) manages dialog state and calls the real API: `submitCreate` POSTs `/api/projects` then `router.push`es to the returned `project.id`'s workspace; `submitRename` PATCHes then `router.refresh()`s; `submitDelete` DELETEs, then `router.push("/editor")` if the deleted project was the currently active workspace (compared via `useParams().projectId`) or `router.refresh()` otherwise. `components/editor/project-sidebar.tsx` now takes `ownedProjects`/`sharedProjects` as props (real `{ id, name }` data, no more mock `owner` field — manage icons shown only for the owned list). Added `app/editor/[projectId]/page.tsx` as a minimal workspace placeholder (not in the original UI specs, but required for "navigate to the new workspace" / "redirect to /editor if deleting the active workspace" to have somewhere real to point at) — server component that 404s via `notFound()` unless the user owns the project or is a matching `ProjectCollaborator`. Per explicit user decision, skipped the spec's separate slug+suffix "Liveblocks room ID" generation (no Liveblocks package or workspace canvas exists yet) — the create dialog's slug preview is cosmetic only, and the real navigated/persisted ID is the Prisma `cuid()` returned by `POST /api/projects`. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean; build output confirms `/editor` and `/editor/[projectId]` are registered.
- `08-editor-workspace-shell.md` — renamed `app/editor/[projectId]/` to `app/editor/[roomId]/` per the spec's route naming (still Prisma's `Project.id` under the hood — no separate room concept exists yet, consistent with the prior decision in `07-wire-editor-home.md`). Since the spec's workspace layout (navbar with project name/share/AI toggle, right AI placeholder) is visually distinct from the existing `/editor` home shell (`EditorShell`/`EditorNavbar`), and Next.js layouts always apply to all nested segments, moved the home route into a route group `app/editor/(home)/{layout,page}.tsx` so `/editor/[roomId]` is a sibling that isn't wrapped by the home `EditorShell`. Added `lib/project-access.ts` (`getCurrentIdentity()` → `{ userId, email }` via Clerk; `hasProjectAccess(project, identity)` → true if `ownerId` matches or a `ProjectCollaborator` row matches `projectId`+`email`) and `components/editor/access-denied.tsx` (centered, lock icon, message, link to `/editor`, using `Button render={<Link .../>}` since this project's `Button` is `@base-ui/react/button`, not Radix — no `asChild`). New `components/editor/workspace-navbar.tsx` and `components/editor/workspace-shell.tsx` (client, mirrors `EditorShell`'s pattern: `ProjectActionsProvider` + `ProjectDialogs` + sidebar-open state, adds `isAiSidebarOpen` state for the right placeholder panel, canvas is a centered-message placeholder). `app/editor/[roomId]/page.tsx` is a server component: explicit `redirect("/sign-in")` if unauthenticated (belt-and-suspenders — `proxy.ts` middleware already blocks this), `AccessDenied` for both a missing project and an existing one the user can't access. `components/editor/project-sidebar.tsx` gained an `activeProjectId` prop (highlights the current room via persistent `bg-accent-dim`) and each `ProjectItem` is now a `next/link` to `/editor/{id}` — previously sidebar items had no navigation at all, so this was needed for "current room highlighted" to be meaningful; `hooks/use-project-actions.tsx`'s `useParams()` key updated from `projectId` to `roomId` to match. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean; build output confirms `/editor` and `/editor/[roomId]` are both registered. Follow-up in the same session (user-requested visual polish, not in the original spec text): redesigned `workspace-navbar.tsx` (title/subtitle stack, outline Share button, pill-style AI toggle) and `workspace-shell.tsx`'s canvas/AI placeholders (icon + eyebrow + heading + description over a subtle CSS grid background; "AI Copilot" panel with a pending-state card and a "Future hooks" note) to match a richer reference design, still placeholder-only.
- `09-share-dialog.md` — `lib/collaborators.ts` (`listCollaborators(projectId)`) queries `ProjectCollaborator` rows then enriches by email via Clerk Backend API's `clerkClient().users.getUserList({ emailAddress: [...] })` (exact match, not partial), building a case-insensitive email → Clerk user map since a project can have collaborators Clerk has no account for — those fall back to email-only per spec. No local user table added. New routes: `app/api/projects/[projectId]/collaborators/route.ts` (`GET` — any user with `hasProjectAccess`, i.e. owner or collaborator, can list; `POST` — owner-only, regex-validates the email, catches Prisma's `P2002` unique-constraint error for a duplicate invite as `409`) and `.../collaborators/[collaboratorId]/route.ts` (`DELETE` — owner-only, 404s if the collaborator row doesn't belong to that project). `components/editor/share-dialog.tsx` is a client dialog reusing `components/ui/dialog.tsx`; owners get an invite form, the collaborator list with remove buttons, and a copy-link row (`Copied!` feedback for 2s) — collaborators only see the read-only list, matching the spec's permission split exactly. Avatars render via `next/image` (added `images.remotePatterns` for `img.clerk.com` in `next.config.ts`) with an initial-letter fallback when no `avatarUrl`. `isOwner` is computed server-side in `app/editor/[roomId]/page.tsx` (`project.ownerId === identity.userId`) and threaded through `workspace-shell.tsx` to both the dialog and gate the UI. Hit this project's `react-hooks/set-state-in-effect` lint rule (forbids a synchronous `setState` call directly in an effect body) three times — fixed by wrapping the collaborator fetch in an inner `async function` called from the effect (the `setIsLoading(true)` call is then inside that function, not the effect's top-level scope) and moving the reset-fields-on-close logic out of a `useEffect` entirely into the `Dialog`'s `onOpenChange` handler, which is an event handler, not an effect. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean; build output confirms both new collaborator routes are registered.

- `10-liveblocks-setup.md` — `liveblocks.config.ts` defines `Presence` (`cursor: { x, y } | null`, `isThinking: boolean`) and `UserMeta` (`id`, `info: { name, avatar, color }`); the scaffold's other placeholder types (`Storage`, `RoomEvent`, `ThreadMetadata`, `RoomInfo`) were changed from `{}` to `Record<string, never>` to satisfy this repo's `@typescript-eslint/no-empty-object-type` lint rule — unrelated to the spec, just needed for `npm run build` to pass. `lib/liveblocks.ts` exports `getLiveblocksClient()` (lazy-memoized `Liveblocks` node client cached on `global`, mirroring `lib/prisma.ts`'s pattern) and `getCursorColor(userId)` (deterministic hash into a fixed 8-color palette). `app/api/liveblocks-auth/route.ts`: requires Clerk auth (`401` if signed out) via `lib/project-access.ts`'s `getCurrentIdentity()`, looks up the `Project` by the posted `room` (Liveblocks room ID = Prisma project ID) and calls the existing `hasProjectAccess()` helper (`403` if missing or unauthorized), calls `getOrCreateRoom` (private by default, `defaultAccesses: []`), then `identifyUser` with name/avatar from Clerk's `currentUser()` and the generated cursor color. Per-request "already installed" spec claim didn't hold: `@liveblocks/node` was missing from `package.json`/`node_modules` (only `client`/`react`/`react-flow`/`react-ui` were present) — installed it (`^3.23.0`). Also added an empty `LIVEBLOCKS_SECRET_KEY=` placeholder to `.env.local` (no key existed) — the real client is lazily constructed (not at module top-level) specifically so `npm run build`'s page-data-collection step doesn't eagerly validate the secret and fail with a blank key; a real key must be filled in before the route works at runtime. This version of `@liveblocks/node`'s `identifyUser` requires `groupIds` on the identity object (not optional as the docs example shows) — passed `[]`. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean; build output confirms `/api/liveblocks-auth` is registered.

- `11-base-canvas.md` — `types/canvas.ts` adds `CanvasNodeData` (`label`, `color`, `shape`) and the `canvasNode`/`canvasEdge` typed aliases (`CanvasNode = Node<CanvasNodeData, "canvasNode">`, `CanvasEdge = Edge<Record<string, never>, "canvasEdge">`) — types only, no custom node/edge renderers registered yet per the spec's scope limits. `components/editor/canvas/canvas-room.tsx` (client) sets up the room: `LiveblocksProvider authEndpoint="/api/liveblocks-auth"` → `RoomProvider id={roomId}` → `ErrorBoundary` (connection-error fallback) → `ClientSideSuspense` (loading fallback) → `Canvas`; also the single place that imports the three required stylesheets (`@xyflow/react/dist/style.css`, `@liveblocks/react-ui/styles.css`, `@liveblocks/react-flow/styles.css`). `components/editor/canvas/canvas.tsx` (client) calls `useLiveblocksFlow<CanvasNode, CanvasEdge>({ suspense: true, nodes: { initial: [] }, edges: { initial: [] } })` from `@liveblocks/react-flow` and passes the synced `nodes`/`edges`/change handlers into `<ReactFlow>` with `connectionMode={ConnectionMode.Loose}`, `fitView`, `<MiniMap />`, and `<Background variant={BackgroundVariant.Dots} />` — no `<Controls />`, matching the "don't add controls yet" scope limit. `components/editor/workspace-shell.tsx`'s placeholder `<main>` (the "Workspace Shell" copy from `08-editor-workspace-shell.md`) is replaced with `<CanvasRoom roomId={projectId} />`; `app/editor/[roomId]/page.tsx` is untouched and still server-side, per spec. `liveblocks.config.ts`'s `Presence.isThinking` (added in `10-liveblocks-setup.md`) is a required field, so `initialPresence` is `{ cursor: null, isThinking: false }` — the spec only mentions `cursor: null`, `isThinking: false` was added to satisfy that existing type rather than making the field optional. Installed missing `react-error-boundary` (spec's dependency list didn't mention it, but `LiveblocksProvider`'s recommended error-fallback pattern requires it and it wasn't in `package.json`). `tsc --noEmit`, `eslint`, and `npm run build` all pass clean; build output confirms `/editor/[roomId]` still builds with the canvas wired in. Not verified in an actual browser: no browser/screenshot tool was available this session, and `LIVEBLOCKS_SECRET_KEY` is still a blank placeholder (see `10-liveblocks-setup.md`), so a real room connection couldn't be exercised end-to-end — confirmed instead via `curl`, which shows the dev server boots and the workspace route redirects unauthenticated requests to sign-in as expected.

- `12-shape-panel.md` — `types/canvas.ts` adds `CANVAS_SHAPES`/`CanvasShape` (`rectangle`/`diamond`/`circle`/`pill`/`cylinder`/`hexagon`), `SHAPE_DEFAULT_SIZES` (rectangle wider than tall, circle square, diamond larger for label room, per spec), `DEFAULT_NODE_COLOR` (`#00c8d4`, matches `--accent-primary`), `CANVAS_SHAPE_DRAG_TYPE` custom drag mime type, and `CanvasShapeDragPayload`; `CanvasNodeData.shape` is now typed as `CanvasShape` instead of `string`. New `components/editor/canvas/shape-panel.tsx` (floating pill toolbar, bottom-center, six draggable icon buttons from `lucide-react` — `RectangleHorizontal`/`Diamond`/`Circle`/`Pill`/`Cylinder`/`Hexagon` — each `dragstart` sets the shape+size payload via `CANVAS_SHAPE_DRAG_TYPE`). New `components/editor/canvas/canvas-node.tsx` (`CanvasNodeRenderer`, the `canvasNode` type's basic renderer for this unit — bordered rectangle with centered label, border color from `data.color`, plus a single top target / bottom source `Handle` so the pre-existing `onConnect` wiring stays functional). `components/editor/canvas/canvas.tsx` restructured: `Canvas` now wraps a new `CanvasInner` in `ReactFlowProvider` (needed because `useReactFlow()`'s `screenToFlowPosition` requires a provider above the component that calls it — the previous single-component version couldn't have called it); `CanvasInner` adds `onDragOver`/`onDrop` on the wrapper div (`dragover` calls `preventDefault` + sets `dropEffect`; `drop` reads the `CANVAS_SHAPE_DRAG_TYPE` payload, converts the pointer position via `screenToFlowPosition`, and appends a new node via `setNodes`), registers `nodeTypes={{ canvasNode: CanvasNodeRenderer }}`, and renders `<ShapePanel />` absolutely positioned over the `<ReactFlow>` canvas. Node IDs are `${shape}-${Date.now()}-${counter}` using a `useRef` counter (per spec: shape name + timestamp + counter). New nodes use an empty `label`, `DEFAULT_NODE_COLOR`, and the dragged shape's value, exactly as specified — no other node fields invented. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean. Not verified in an actual browser (no browser/screenshot tool available this session, same limitation noted in `11-base-canvas.md`) — drag-and-drop interaction and the on-canvas visual result are unconfirmed end-to-end.

- `13-node-shape.md` — new `components/editor/canvas/shape-visual.tsx` (`ShapeVisual`) is the single shape-rendering component, shared by both the real node renderer and the drag preview: rectangle/pill/circle render as a styled `div` (border-radius `rounded-md`/`rounded-full`, `bg-surface`), diamond/hexagon/cylinder render as an SVG (`viewBox="0 0 100 100"`, `preserveAspectRatio="none"` so the shape stretches to fill the node's actual `width`/`height` — diamond/hexagon are `<polygon>`s, cylinder is two `<path>`s forming the classic body+lid outline). Border/stroke color is `data.color` at ~30% alpha (hex + `4d` suffix) at rest and full opacity + a 1px `box-shadow`/`drop-shadow` ring when `selected`, per spec. `components/editor/canvas/canvas-node.tsx` (`CanvasNodeRenderer`) now just wraps `ShapeVisual` with the existing top/bottom `Handle`s — no rendering logic of its own. Added a ghost drag preview: new `components/editor/canvas/shape-drag-preview.tsx` (`ShapeDragPreview`, a `fixed`, `pointer-events-none`, cursor-centered `ShapeVisual` at the dragged shape's default size/color) and drag-position state lifted into `components/editor/canvas/canvas.tsx`'s `CanvasInner` (`dragPayload`/`dragPos`, set via new `onDragStart`/`onDragEnd` props threaded through `shape-panel.tsx`'s existing per-button `dragstart`/`dragend` handlers). Since HTML5 `dataTransfer.getData()` isn't readable during `dragover` (browser security restriction — only `dragstart`/`drop` can read payload data), the preview's shape/size come from the `onDragStart` payload captured once at drag start, and only its `x`/`y` position is updated continuously via a `document`-level `dragover` listener (attached only while `dragPayload` is set) — this also means the preview correctly follows the cursor across both the shape panel and the canvas, since `dragover` bubbles from whatever element is currently under the pointer. The panel's native browser drag ghost (which would otherwise show the dragged icon button, not the shape) is suppressed via `event.dataTransfer.setDragImage()` on a transparent 1x1 GIF `Image`. Preview clears on both `dragend` (covers drag-cancelled) and on `drop` (`canvas.tsx`'s `onDrop` explicitly clears `dragPayload`, since `dragend` firing immediately after `drop` was not guaranteed to be visually instant enough on its own). No changes to shape panel layout, node-creation logic, or resize/label editing, per scope limits. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean. Not verified in an actual browser (same `LIVEBLOCKS_SECRET_KEY`/no-browser-tool limitation as `11-base-canvas.md`/`12-shape-panel.md`) — shape rendering and drag-preview behavior are unconfirmed end-to-end visually.

- `14-node-editing.md` — added resize and inline label editing, both inside `components/editor/canvas/canvas-node.tsx` (`CanvasNodeRenderer`). Resize: `NodeResizer` (from `@xyflow/react`, already bundled — no new dependency) rendered with `isVisible={selected}`, `minWidth`/`minHeight` from a new `MIN_NODE_SIZE` constant in `types/canvas.ts` (`{ width: 40, height: 40 }`, chosen below every `SHAPE_DEFAULT_SIZES` entry so no shape's default size is itself invalid), and handle/line colors styled subtly via `--accent-primary` at low opacity (small `!size-2` square handles, thin low-opacity connecting lines) rather than the library default blue. Label editing: double-clicking the node sets local `isEditing` state (`useState`, per-node-instance — no new global/collab state needed since only one node edits at a time locally and the label itself is already collaborative), which renders a `nodrag nopan nowheel` absolutely-positioned `<textarea>` on top of the node (those three xyflow utility classNames are what stop the text interaction from being reinterpreted as a node-drag or pane-pan/zoom gesture — confirmed via `@xyflow/react`'s own docs/source, this is the standard escape hatch, not a new mechanism). The textarea is centered via `flex items-center justify-center` directly on the `textarea` element itself (verified this works for centering the text/placeholder in Chromium/Firefox despite `textarea` being a replaced element). Both resize and label edits go through `useReactFlow()`'s `updateNodeData`/the resizer's internal node-change dispatch — same store-level mechanism already used by the existing `onNodesChange` prop wired to `useLiveblocksFlow`, so both flow through the existing collaborative sync path with no new plumbing. `components/editor/canvas/shape-visual.tsx` gained a `hideLabel` prop (suppresses the rendered label span while the textarea overlay is showing, so the two don't visually double up) and a shared `LabelText` helper that renders the label when non-empty or a muted "Label" placeholder in the same centered position when empty (both editing and non-editing empty states use it, so there's no layout shift switching between them — the node's box size is unchanged either way, only the overlay is added/removed). Escape blurs the textarea (closing edit mode via the existing `onBlur` handler, no separate revert — labels already commit live on every keystroke via `updateNodeData`, matching the spec's "update the label as users type"). No changes to shape rendering logic, the shape panel, drag preview, or node-creation flow, per scope limits. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean. Not verified in an actual browser (same `LIVEBLOCKS_SECRET_KEY`/no-browser-tool limitation as prior canvas units) — resize handles, drag-to-resize, and the double-click-to-edit textarea overlay are unconfirmed end-to-end visually.

- `15-node-color-toolbar.md` — `types/canvas.ts` adds `NODE_COLORS` (8 background/text pairs, matching `context/ui-context.md`'s "Node Color Palette" table exactly) and `NodeColorPair`; the palette isn't in `globals.css`'s theme tokens (only single-role colors like `--accent-primary` live there, not paired node colors), so per the spec's fallback instruction it was added to `types/canvas.ts` instead. `CanvasNodeData` gains a `textColor` field alongside the existing `color` field — `color` is now the node's actual background fill (previously it was only used as a border/accent color drawn over a fixed `bg-surface` fill). `DEFAULT_NODE_COLOR`/new `DEFAULT_NODE_TEXT_COLOR` now derive from `NODE_COLORS[0]` (`#1F1F1F`/`#EDEDED`, the "Neutral dark (default)" row) instead of the previous standalone cyan `#00c8d4` — this changes newly-created nodes' default look (dark gray fill instead of a cyan-bordered `bg-surface` node) to match the palette documented as canonical in `ui-context.md`; `components/editor/canvas/shape-drag-preview.tsx`'s ghost preview updated to match. `components/editor/canvas/shape-visual.tsx` (`ShapeVisual`) now fills shapes with `color` (was hardcoded `var(--bg-surface)`/`bg-surface`) and sets label text color to the new `textColor` prop; border/stroke and the selected-state ring/glow now derive from `textColor` (falls back to `color` if absent) rather than `color` itself, since the vivid palette text colors read better as accents than the dark fills do. New `components/editor/canvas/node-color-toolbar.tsx` (`NodeColorToolbar`) is a small pill of 8 circular swatch buttons (`size-5`, one per `NODE_COLORS` pair) absolutely positioned via `-top-12 left-1/2 -translate-x-1/2` relative to the node's own wrapper div (already `position: relative`), so it floats above the node without overlapping regardless of node size; active swatch shows a persistent 2px ring in its own text color, hover adds a small (4px) glow in the same color — both computed as a combined inline `box-shadow` via local `useState` hover tracking (chosen over CSS `:hover` classes since the active ring is also set via inline `style`, which would otherwise out-specificity a Tailwind hover class and hide the hover glow on the active swatch). Drag/pan prevention uses the same `nodrag nopan nowheel` xyflow escape-hatch classNames already used for the label-edit textarea in `14-node-editing.md`, plus `stopPropagation` on `mousedown`/`pointerdown`/`click` for safety. `components/editor/canvas/canvas-node.tsx` (`CanvasNodeRenderer`) renders `<NodeColorToolbar>` only `{selected && ...}`, wires its `onSelect` to a new `handleColorSelect` callback that calls the existing `updateNodeData(id, { color: pair.background, textColor: pair.text })` — same `useReactFlow()` mechanism already used for label edits and resize, so this flows through the existing Liveblocks-synced `onNodesChange` path with no new plumbing and no server calls, matching the spec's "keep this inside the existing collaborative canvas state" requirement. No changes to node selection logic, drag/drop, or resize — only the toolbar, the color-pair data, and how that data is rendered. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean. Not verified in an actual browser (same `LIVEBLOCKS_SECRET_KEY`/no-browser-tool limitation noted in prior canvas units) — swatch hover glow, active-ring styling, and toolbar positioning are unconfirmed end-to-end visually.

- `16-edge-behavior.md` — `components/editor/canvas/canvas-node.tsx` (`CanvasNodeRenderer`) now renders four `Handle`s (`top`/`right`/`bottom`/`left`, all `type="source"` — connection mode is already `ConnectionMode.Loose`, set in `11-base-canvas.md`, which lets any handle connect to any other handle regardless of declared type, so a single type per handle is sufficient for "connect from any handle to any other handle"), styled as small white dots with a dark border (`!bg-white`, `!border-[#1f1f1f]`, `!size-2 !rounded-full`) that are `!opacity-0` by default and fade in via a new `group`/`group-hover:!opacity-100` pair on the node's wrapper div (the wrapper previously had no `group` class). New `components/editor/canvas/canvas-edge.tsx` (`CanvasEdgeRenderer`) is the `canvasEdge` edge type: uses `getSmoothStepPath({ ..., borderRadius: 0 })` for clean right-angle routing, renders a `BaseEdge` whose stroke/opacity are driven by local `isHovered` state (`onMouseEnter`/`onMouseLeave` on a wrapping `<g>`) or the `selected` prop — dimmed `var(--border-subtle)` at rest, full-opacity `var(--accent-primary)` on hover/select — plus a separate invisible wide-stroke (`strokeWidth={20}`, `strokeOpacity={0}`) sibling `<path>` layered underneath purely to enlarge the hover/click hit area without touching the visible line's `strokeWidth`. Label editing uses `EdgeLabelRenderer` positioned at the `labelX`/`labelY` values `getSmoothStepPath` already returns (no manual midpoint math, per spec) inside a `nodrag nopan nowheel` wrapper div (same xyflow escape-hatch classNames used for node label editing in `14-node-editing.md`) with `stopPropagation` on `mousedown`, so label clicks/typing never pan or drag the canvas; double-click enters edit mode (a growing `<input>` sized via inline `style={{ width: `${label.length}ch` }}`), saving on every keystroke through `useReactFlow().updateEdgeData(id, { label })` — same store-level mechanism `updateNodeData` already uses for node labels, so it flows through the existing Liveblocks-synced `onEdgesChange` path with no new plumbing; blur closes edit mode, Enter/Escape both blur (no separate revert, consistent with the node label editing pattern). Saved non-empty labels render as a small pill badge (`rounded-full border border-surface-border bg-surface`); an active (hovered-or-selected) edge with no label shows a faint dashed-border "Double-click to label" hint instead. `types/canvas.ts`'s `CanvasEdge` type gained a `CanvasEdgeData` (`{ label?: string }`) generic param (was `Record<string, never>`, i.e. no edge data existed before this unit). `components/editor/canvas/canvas.tsx` registers `edgeTypes={{ canvasEdge: CanvasEdgeRenderer }}` and a new `defaultEdgeOptions` (`type: "canvasEdge"`, `markerEnd: { type: MarkerType.ArrowClosed, color: "var(--border-subtle)" }`) on `<ReactFlow>` so every new connection (via the existing `onConnect` prop, untouched) automatically gets the custom renderer and an arrowhead — matches this file's existing pattern of passing CSS custom properties directly into xyflow style/color props (e.g. `--accent-primary` in `NodeResizer`'s `color`), so `var(--border-subtle)` resolves the same way. No changes to node creation, the shape panel, or the node renderer beyond the four handles, per scope limits. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean. Not verified in an actual browser (same `LIVEBLOCKS_SECRET_KEY`/no-browser-tool limitation noted in every canvas unit since `11-base-canvas.md`) — handle hover-fade, edge routing/hover/selection styling, and the inline label editing interaction are unconfirmed end-to-end visually.

## Completed (continued)

- `24-ai-presence-state.md` — new `types/tasks.ts` defines `AI_STATUS_FEED_ID` (`"ai-status-feed"`), `AI_STATUS_VALUES`/`AiStatus`, and a zod `aiStatusFeedMessageSchema` (`{ status: enum, text?: string }`) plus `parseAiStatusFeedMessage()` for runtime validation of incoming feed data (feed payloads cross the wire as untyped JSON, so the existing `Liveblocks` global TS types alone don't guarantee shape at runtime). Investigated `@liveblocks/node`/`@liveblocks/react`'s `.d.ts` files directly (per this spec's "check the existing Liveblocks setup" instruction) and confirmed Liveblocks has a first-class **Feeds** primitive (`createFeed`/`createFeedMessage` server-side, `useFeeds`/`useFeedMessages`/`useCreateFeedMessage` client-side) — a different, more recently-added mechanism than the `broadcastEvent`/`useEventListener` RoomEvent approach `23-design-agent-logic.md` had used for its own `"ai-status"` banner. Per this spec's explicit "follow Liveblocks best practices for feeds/presence instead of creating parallel realtime state" instruction, migrated onto Feeds rather than adding a second parallel status channel: `liveblocks.config.ts`'s `RoomEvent` reverted to `Record<string, never>` (now unused) and gained `FeedMessageData: AiStatusFeedMessage`; `trigger/design-agent.ts`'s `publishStatus()` now calls `client.createFeedMessage({ roomId, feedId: AI_STATUS_FEED_ID, data: { status, text: message } })` (after an idempotent best-effort `client.createFeed(...).catch(() => {})` at the top of the run, since Liveblocks requires a feed to exist before messages can be added to it and the API errors on a duplicate `createFeed` call); `components/editor/canvas/ai-status-banner.tsx` (from `23-design-agent-logic.md`) now reads the feed via the new shared hook instead of `useEventListener`. New `hooks/use-ai-status-feed.ts` (`useLatestAiStatus`) wraps `useFeedMessages(AI_STATUS_FEED_ID)` (regular, non-suspense import from `@liveblocks/react`, matching this file's other presence hooks) and returns the first (newest — Liveblocks sorts feed messages newest-first) message run through `parseAiStatusFeedMessage`, or `null` while loading/on error/if invalid — this is the single validation point every consumer goes through. Since `useFeedMessages` requires a `RoomContext` and the existing `AiSidebar` rendered as a **sibling** of `CanvasRoom`'s `LiveblocksProvider`/`RoomProvider` (outside the room entirely, per `20-ai-sidebar-shell.md`), this required lifting the provider: `components/editor/canvas/canvas-room.tsx` no longer wraps `LiveblocksProvider`/`RoomProvider` itself (now just `ErrorBoundary` + `ClientSideSuspense` + `Canvas`, with its `roomId` prop renamed to `projectId` to match `Canvas`'s own prop naming since the room-identifying purpose moved elsewhere); `components/editor/workspace-shell.tsx` now renders `LiveblocksProvider authEndpoint="/api/liveblocks-auth"` → `RoomProvider id={projectId} initialPresence={{ cursor: null, thinking: false }}` around the entire `<main>`/`<aside>` row (`ProjectSidebar`, `CanvasRoom`, and `AiSidebar` are now all inside one room context) — matches this skill's own documented "lift the provider so canvas and aside share one room" pattern, rather than connecting to the room twice. `AiSidebar` calls `useLatestAiStatus()` directly (no suspense boundary added around it, since the regular hook returns `null` while loading and the spec explicitly says "keep the rest of the sidebar usable" / "don't block or dim the whole sidebar") and derives `isGenerating` from `status === "start" || status === "processing"`; when true it renders a small spinner+text status strip below the sidebar header (visible to every participant with the sidebar open, since the feed is shared room state, not local), disables the `Textarea` and starter-prompt chips, and disables the send button (`Button`) while swapping its `Send` icon for a spinning `Loader2`. `components/editor/canvas/live-cursors.tsx`'s existing `presence.thinking` text suffix (`" · thinking…"`, added in `23-design-agent-logic.md`) is now a `lucide-react` `Loader2` spinner icon instead of text, shown only `{other.presence.thinking ? ... : null}` — presence itself (`Presence.thinking`, from `10-liveblocks-setup.md`) was already wired correctly and needed no changes, only how the cursor badge renders it. No changes to node/edge rendering, the shape panel, or `trigger/design-agent.ts`'s actual generation/tool logic — only the status-transport mechanism, per the "don't add actual AI generation logic yet" / "don't trigger background tasks yet" scope limits (the task already existed from `22-design-agent-api.md`/`23-design-agent-logic.md`; nothing new triggers it). `tsc --noEmit`, `eslint` (scoped to touched files — see the `.trigger/tmp` note in `23-design-agent-logic.md` for why a full repo-wide `npm run lint` isn't run), and `npm run build` all pass clean; build output confirms every existing route still registers. Hit this repo's `react-hooks/set-state-in-effect` rule (also called out in `09-share-dialog.md`) once in `ai-status-banner.tsx`'s auto-dismiss logic — fixed by storing the dismissed message itself (compared by value, not a boolean reset via a second effect) so the timeout's `setTimeout(() => setDismissed(latest), 4000)` is the only place `setDismissed` is called, and it's inside an async callback, not synchronously in the effect body. Not verified in an actual browser (same `LIVEBLOCKS_SECRET_KEY`/no-browser-tool limitation noted in every canvas/workspace unit since `11-base-canvas.md`, compounded here by also needing a live `trigger dev` run to produce real feed messages) — the feed schema, provider restructuring, sidebar disabled/loading states, and cursor spinner are verified by type-checking and code inspection only.

## In Progress

- `17-canvas-ergonomics.md` — new `components/editor/canvas/canvas-controls.tsx` (`CanvasControls`) is a pill-shaped control bar (matches `shape-panel.tsx`'s existing `rounded-full border border-surface-border bg-surface` styling), absolutely positioned `bottom-24 left-6` (bottom-left, stacked above the bottom-center `ShapePanel`) with two icon-button groups separated by a thin `bg-surface-border` divider: zoom out/fit view/zoom in (`lucide-react`'s `ZoomOut`/`Maximize`/`ZoomIn`), then undo/redo (`Undo2`/`Redo2`). Zoom handlers come from `useReactFlow()`'s `zoomIn`/`zoomOut`/`fitView`, each called with `{ duration: 200 }` for a short animated transition. Undo/redo and their disabled state use `@liveblocks/react`'s `useUndo`/`useRedo`/`useCanUndo`/`useCanRedo` hooks directly against the room's existing Liveblocks history (no new state) — disabled buttons get `disabled:opacity-40 disabled:pointer-events-none` on the shared `ControlButton` sub-component. New `hooks/useKeyboardShortcuts.ts` (generic over xyflow's `Node`/`Edge` types to match `CanvasInner`'s typed `useReactFlow<CanvasNode, CanvasEdge>()` return value without a type-widening cast) takes the React Flow instance plus `onUndo`/`onRedo` callbacks, attaches a single `keydown` listener on `window`, and bails out early via an `isEditableTarget()` check (`INPUT`/`TEXTAREA`/`isContentEditable`) so shortcuts don't fire while a node/edge label textarea or input is focused (reuses the same editable-field concept already relied on implicitly by `14-node-editing.md`'s label textarea, just centralized here). Shortcuts: `+`/`=` → `zoomIn`, `-` → `zoomOut` (both only when no Cmd/Ctrl modifier, so they don't collide with browser page-zoom bindings), `Cmd/Ctrl+Z` → undo, `Cmd/Ctrl+Shift+Z` → redo, `Cmd/Ctrl+Y` → redo — all call `event.preventDefault()` to stop the browser's own undo/zoom behavior. `components/editor/canvas/canvas.tsx`'s `CanvasInner` wires all of this together: `useReactFlow()`'s full instance is now kept (previously only `screenToFlowPosition` was destructured out of it) so it can be passed whole into `useKeyboardShortcuts`; `<CanvasControls>` renders alongside the existing `<ShapePanel>`. No changes to the shape panel, node/edge renderers, or the Liveblocks room/provider setup, per scope limits. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean. Not verified in an actual browser (same `LIVEBLOCKS_SECRET_KEY`/no-browser-tool limitation noted in every canvas unit since `11-base-canvas.md`) — control bar layout, zoom/undo/redo button behavior, and keyboard shortcut handling are unconfirmed end-to-end visually.

- `18-starter-templates.md` — new `components/editor/starter-templates.ts` defines `CanvasTemplate`/`CanvasTemplateNode`/`CanvasTemplateEdge` and `CANVAS_TEMPLATES` (three templates — Microservices, CI/CD Pipeline, Event-Driven System — built via small local `node()`/`edge()` helpers), reusing `types/canvas.ts`'s `CanvasShape`, `SHAPE_DEFAULT_SIZES`, and `NODE_COLORS` so template nodes use the same shape sizing and palette as manually-created nodes. New `components/editor/starter-templates-modal.tsx` (`StarterTemplatesModal`) is a `Dialog` (widened via `sm:max-w-2xl` since the default `Dialog` content is `sm:max-w-sm`) with a `ScrollArea`-wrapped grid of `Card`s, one per template, each showing name/description and an `Import` button; the preview is a lightweight `TemplatePreview` (plain absolutely-positioned `div`s for nodes + a plain `<svg>` of `<line>`s for edges, no `ShapeVisual`/React Flow instance involved) that computes a bounding box from template node positions/sizes and maps it into a fixed `260x140` viewport via a local `computePreviewLayout()` (scale-to-fit + center, clamped to `scale <= 1`). Clicking `Import` calls the `onImport` prop then closes the dialog itself (`onOpenChange(false)`), matching the spec's "call onImport, then close" order. Canvas wiring required threading an imperative handle through the existing Liveblocks-nested tree since the navbar's new Templates button lives in `workspace-shell.tsx`, outside `CanvasRoom`'s `LiveblocksProvider`/`RoomProvider`, while the actual node/edge state only exists inside `canvas.tsx`'s `CanvasInner` (via `useLiveblocksFlow`): `canvas.tsx` now exports a `CanvasHandle` (`{ importTemplate(template) }`) type, and both `CanvasInner` and the outer `Canvas` are `forwardRef` components exposing it via `useImperativeHandle`; `canvas-room.tsx`'s `CanvasRoom` is now `forwardRef` too, just passing the ref through to `<Canvas ref={ref} />`. `importTemplate()` reads the current nodes/edges fresh via `reactFlowInstance.getNodes()/getEdges()` (avoids stale-closure issues from putting this in an effect), removes them all via `onNodesChange`/`onEdgesChange` with `type: "remove"` changes, adds the template's nodes/edges via `type: "add"` changes (same Liveblocks-synced `onNodesChange`/`onEdgesChange` mechanism every other canvas mutation already goes through — `12-shape-panel.md`'s node creation, `15-node-color-toolbar.md`'s color changes, etc. — so no new sync plumbing), then calls `fitView({ duration: 200 })` inside a `requestAnimationFrame` (a plain synchronous `fitView()` call right after adding nodes measures stale/zero-sized nodes since React hasn't re-rendered them into the DOM yet). `workspace-shell.tsx` holds `isTemplatesOpen` state and a `canvasRef = useRef<CanvasHandle>(null)` passed to `<CanvasRoom ref={canvasRef} .../>`; `StarterTemplatesModal`'s `onImport` calls `canvasRef.current?.importTemplate(template)`. `workspace-navbar.tsx` gained an `onOpenTemplates` prop and a `Templates` button (`LayoutTemplate` icon) next to the existing Share button. No changes to node/edge rendering components (`canvas-node.tsx`, `canvas-edge.tsx`, `shape-visual.tsx`) or to how manually-created nodes/edges are built, per scope limits — templates only produce data in the same `CanvasNode`/`CanvasEdge` shape those renderers already handle. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean. Not verified in an actual browser (same `LIVEBLOCKS_SECRET_KEY`/no-browser-tool limitation noted in every canvas unit since `11-base-canvas.md`) — the modal's preview rendering, the import/clear/fit-view flow, and the Templates button are unconfirmed end-to-end visually.

- `19-presence-avatars-cursors.md` — `liveblocks.config.ts`'s `Presence.isThinking` renamed to `thinking` to match the spec's exact field name (only other reference was `canvas-room.tsx`'s `initialPresence`, updated alongside it). New `components/editor/canvas/presence-avatars.tsx` (`PresenceAvatars`) reads `useOthers()` (`@liveblocks/react`) and Clerk's `useUser()`, filters `others` by `other.id !== user?.id` per the spec (redundant with Liveblocks' own self-exclusion in `useOthers`, but implemented anyway since the spec calls it out explicitly), renders up to 5 overlapping avatar circles (`next/image` when `info.avatar` is set, initials fallback otherwise, `ring-2 ring-base` per spec) plus a `+N` chip, a `bg-surface-border` divider shown only when `collaborators.length > 0`, and the existing Clerk `UserButton` sized to match via `appearance.elements.userButtonAvatarBox` (both fixed at 28px). New `components/editor/canvas/live-cursors.tsx` (`LiveCursors`) reads `useOthers()` and renders a `lucide-react` `MousePointer2` + name badge per other participant with a `presence.cursor`, colored via `other.info.color`; positions are stored in flow (world) coordinates (not raw screen pixels) so cursors line up correctly across viewers with different pan/zoom, converted back to on-screen pixels per-render via `useReactFlow().flowToScreenPosition()`. `components/editor/canvas/canvas.tsx`'s `CanvasInner` wires the broadcast side: `useUpdateMyPresence()` plus a `screenToFlowPosition`-based handler passed as `onMouseMove`/`onMouseLeave` directly on `<ReactFlow>` (its prop type extends `HTMLAttributes<HTMLDivElement>`, so this needed no wrapper-div plumbing), clearing `cursor: null` on mouse leave; `<LiveCursors />` and `<PresenceAvatars />` render as siblings of `<ReactFlow>` inside the same relative-positioned canvas container, so this presence UI only ever appears in the canvas/room view, never the editor home navbar or `workspace-navbar.tsx` (both untouched, per scope limits — the existing navbar's own `UserButton` and Share/Templates/AI actions are unchanged). No changes to node/edge behavior. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean. Not verified in an actual browser (same `LIVEBLOCKS_SECRET_KEY`/no-browser-tool limitation noted in every canvas unit since `11-base-canvas.md`) — avatar stack layout/overflow, the divider condition, and live cursor positioning/color are unconfirmed end-to-end visually with a second real participant.

- `20-ai-sidebar-shell.md` — new `components/editor/ai-sidebar.tsx` (`AiSidebar`) extracts the AI panel out of `workspace-shell.tsx`'s inline placeholder `<aside>` into its own component, taking `isOpen`/`onClose` as props (state still owned by `workspace-shell.tsx`, unchanged — only the rendering moved). The previous placeholder was a `shrink-0` flex child that collapsed via `transition-[width]` (pushing the canvas), which didn't match the spec's description of an already-floating slide-in sidebar; the spec's own listed styles (`bg-base/95`, shadow treatment, slide-in "from the right side") match this codebase's other floating sidebar, `project-sidebar.tsx` (`absolute` + `shadow-xl` + `transition-transform` + `translate-x-*`), just mirrored to the right edge — so `AiSidebar` now renders as `absolute top-0 right-0 z-40 ... bg-base/95 shadow-xl transition-transform duration-200 ease-out`, sliding via `translate-x-0`/`translate-x-full`, positioned (like `ProjectSidebar`) inside the same `relative flex flex-1 overflow-hidden` container as `<main>`, so it floats over the canvas rather than resizing it. Header: small `Bot` icon in a `bg-accent-dim`/`text-brand` circle, `AI Workspace` title (`text-copy-primary`), `Collaborate with Ghost AI` subtitle (`text-copy-muted`), close button (`Button variant="ghost" size="icon-sm"`, same as `ProjectSidebar`'s close button). Tabs use the existing shadcn `Tabs`/`TabsList`/`TabsTrigger`/`TabsContent` (`components/ui/tabs.tsx`, `@base-ui/react/tabs`-based) with `TabsTrigger` styled via its `data-active:` state instead of the component's own default active styles. Per spec step 6 ("use the existing project color tokens... avoid inventing new colors"), the spec's own literal class names don't all exist in this repo's Tailwind config (checked `app/globals.css` and `context/ui-context.md` first) — mapped them onto the real equivalents: `text-primary-text`→`text-copy-primary`, `text-muted-text`→`text-copy-muted`, `bg-accent`/`text-accent` (active tab)→`bg-accent-dim`/`text-brand` (this repo's actual brand-accent pair, not shadcn's neutral `--accent`), `text-accent-text`→`text-brand`, `bg-brand-dim`→`bg-accent-dim` (same `--accent-primary-dim` variable, different alias); `border-brand/50`, `bg-subtle`, `bg-elevated`, `border-surface-border`, `text-copy-primary`, `bg-base/95` all already exist under those exact names and were used as-is. AI Architect tab: local-only `useState<ChatMessage[]>` (`role: "user" | "assistant"`, only `"user"` is ever produced — no assistant replies are generated, per the "don't add AI generation logic yet" scope limit) backs a scrollable message list; empty state shows a `Bot` icon, short description, and the three spec-exact starter prompt chips (`bg-subtle`/`text-brand` soft pills) which append themselves as a user message on click; the `Textarea` (shadcn, already `field-sizing-content` for auto-resize) is clamped via `min-h-[72px] max-h-[160px]` (twMerge overrides the component's own `min-h-16` default); `Enter` submits (`preventDefault` + send) and `Shift+Enter` falls through to the textarea's default newline behavior, matching spec exactly; send button is `bg-brand text-white`, disabled when the input is blank. Specs tab: a `Generate Spec` button (`bg-brand text-white`, no `onClick` — intentionally inert, no backend/generation logic per scope limits) and one static demo spec card (plain `div`, not shadcn `Card`, matching this repo's existing precedent for bordered info cards — e.g. the old AI-panel placeholder and `share-dialog.tsx` — since `Card`'s default `ring-1 ring-foreground/10` conflicts with the spec's plain `border-surface-border` look) with a `FileText` icon, a title, a short snippet, and a disabled `Download` icon-button. No Liveblocks or AI logic added; `workspace-navbar.tsx`'s AI toggle button and `isAiSidebarOpen` state in `workspace-shell.tsx` are unchanged, only what they render changed. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean. Not verified in an actual browser (same `LIVEBLOCKS_SECRET_KEY`/no-browser-tool limitation noted in every canvas/workspace unit since `11-base-canvas.md`) — the slide-in animation, tab active styling, chat bubble layout, and starter-chip interaction are unconfirmed end-to-end visually.

- `21-canvas-autosave.md` — reused the existing `Project.canvasJsonPath` field (from `05-prisma.md`) to store the canvas blob URL rather than adding a new column — it was already unused and matches the spec's "add or reuse a field" instruction; Prisma still only holds metadata, the field just holds a URL string, not a filesystem path. New `app/api/projects/[projectId]/canvas/route.ts`: `PUT` auth-checks via `getCurrentIdentity`/`hasProjectAccess` (`lib/project-access.ts`, same helper `06-project-apis.md`/`09-share-dialog.md` routes use — owner or collaborator, not owner-only, since any active collaborator should be able to autosave), uploads the posted `{ nodes, edges }` JSON to Vercel Blob via `put()` at a stable path `canvas/${projectId}.json` with `addRandomSuffix: false` + `allowOverwrite: true` (so repeated autosaves overwrite in place instead of accumulating blobs), then writes the returned `blob.url` to `Project.canvasJsonPath`; `GET` does the same access check, returns `{ nodes: [], edges: [] }` if no saved blob yet, otherwise reads the blob via `@vercel/blob`'s `get(pathname, { access: "private" })` and returns its JSON body as-is. New `hooks/use-canvas-autosave.ts` (`useCanvasAutosave`) takes `{ projectId, nodes, edges }`, skips the very first effect run (mount) via a `useRef` guard so it doesn't immediately PUT on load, then debounces 1500ms after any `nodes`/`edges` reference change before PUTing and returns a `SaveStatus` (`"idle" | "saving" | "saved" | "error"`). Load-on-mount logic lives directly in `components/editor/canvas/canvas.tsx`'s `CanvasInner` (not a separate hook — it needs `onNodesChange`/`onEdgesChange` from the same `useLiveblocksFlow` call already there): captures `nodes.length`/`edges.length` at mount via `useState(() => ...)` lazy initializers (so the check reflects the room's state at first render, not a later one, without triggering `react-hooks/exhaustive-deps` complaints), and only if both were `0` does it `GET` the saved canvas and add any returned nodes/edges via the existing `onNodesChange`/`onEdgesChange` `"add"`-change mechanism (same pattern `18-starter-templates.md`'s `importTemplate` already uses) — if the room already had content, the fetch never fires, satisfying "skip the load entirely to avoid overwriting active collaboration". `CanvasInner`/`Canvas`/`CanvasRoom` (`canvas.tsx`, `canvas-room.tsx`) all gained a `projectId` prop (`CanvasRoom`'s existing `roomId` — already documented as == the Prisma project id, per `10-liveblocks-setup.md` — is passed through as `projectId` rather than adding a redundant second identifier) and an optional `onSaveStatusChange` callback threaded down to report the autosave hook's `SaveStatus` back up, since the status needs to reach the navbar, which lives outside the Liveblocks/canvas tree. New `components/editor/canvas/save-status-indicator.tsx` (`SaveStatusIndicator`) renders nothing for `"idle"`, otherwise a small pill (matches `shape-panel.tsx`/`canvas-controls.tsx`'s existing `rounded-full border border-surface-border bg-surface` styling) with a spinning `Loader2`/`Check`/`AlertCircle` icon + label; there was no pre-existing "editor Save button" in `workspace-navbar.tsx` to attach an indicator to (autosave has no manual trigger anywhere in this codebase), so per the spec's literal instruction it's rendered as its own status pill in the navbar's right-hand button group (left of Templates/Share) rather than inventing a manual Save button the spec never asked for. `workspace-shell.tsx` now owns the `saveStatus` state (`useState<SaveStatus>("idle")`), passing `setSaveStatus` into `CanvasRoom`'s `onSaveStatusChange` and `saveStatus` into `WorkspaceNavbar`. Installed `@vercel/blob`; `BLOB_READ_WRITE_TOKEN` was already present in `.env.local` (pre-existing, not added this session). No changes to node/edge rendering, the shape panel, or Liveblocks room setup, per scope limits. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean; build output confirms `/api/projects/[projectId]/canvas` is registered. Not verified in an actual browser (same `LIVEBLOCKS_SECRET_KEY`/no-browser-tool limitation noted in every canvas unit since `11-base-canvas.md`) — the debounced autosave timing, the load-on-empty-room fetch, and the save status pill's states are unconfirmed end-to-end visually; a real `LIVEBLOCKS_SECRET_KEY` is still needed to open a real room at all.
  - Follow-up fix (runtime error surfaced from a manually-run dev server, not caught by `npm run build`): `put()` was called with `access: "public"`, but this project's actual Vercel Blob store is configured `private` — `PUT /api/projects/[projectId]/canvas` threw `Vercel Blob: Cannot use public access on a private store`. Switched both directions to `access: "private"`: `PUT` now uploads with `access: "private"`, and `GET` now reads via `@vercel/blob`'s `get(pathname, { access: "private" })` (returns an authenticated `{ stream, blob }`, buffered via `new Response(result.stream).text()` then `JSON.parse`) instead of a raw unauthenticated `fetch(project.canvasJsonPath)`, which would have 403'd against a private blob the same way the plain public URL fetch would have failed once traffic actually hit a private store. `tsc --noEmit`, `eslint`, and `npm run build` still pass clean after the fix.

- `22-design-agent-api.md` — Trigger.dev, `trigger.config.ts`, and `trigger/example.ts` were already scaffolded in this repo (not part of this unit — pre-existing at session start) with `TRIGGER_SECRET_KEY` already set in `.env.local`; reused that setup as-is per the spec's "check the existing setup first" instruction — no new pattern introduced. New `prisma/models/task-run.prisma` adds `TaskRun` (`runId` unique, `projectId`, `userId`, `createdAt`, `@@index([runId])`, `@@index([userId, projectId])`) — migration `prisma/migrations/20260728093148_add_task_run` applied cleanly via `prisma migrate dev` against the pooled Prisma Postgres URL (the local-dev-server `P1017` quirk noted in Session Notes didn't recur here, since `DATABASE_URL` in this environment points at the pooled remote, not `prisma dev`). New `trigger/design-agent.ts` exports `designAgent` (`id: "design-agent"`), a minimal task that just `logger.log`s and echoes back its `{ prompt, roomId }` payload — no AI logic, per scope limits. New `app/api/ai/design/route.ts`: `POST` auth-checks via `getCurrentIdentity`/`hasProjectAccess` (`lib/project-access.ts`, same helper every other project route uses), validates `{ prompt, roomId, projectId }` from the body, triggers `design-agent` via `tasks.trigger<typeof designAgent>(...)` using a type-only import (per the SDK's own "don't import the task instance into backend code" rule), creates a `TaskRun` row (`runId` = the returned handle's `id`, `projectId`, `userId` from Clerk), and returns `{ runId }`. New `app/api/ai/design/token/route.ts`: `POST` auth-checks, accepts `{ runId }`, looks up the `TaskRun` by `runId` and 403s unless `taskRun.userId` matches the caller (ownership check is against the `TaskRun` record itself, not project access, since the spec calls this out as a separate verification step), mints a run-scoped token via `auth.createPublicToken({ scopes: { read: { runs: [runId] } } })` (`@trigger.dev/sdk`'s `auth` export, per the realtime-and-frontend skill — a token scoped to just that one run, not a blanket read token), and returns `{ token }`. No node/edge generation, no AI provider calls, no canvas updates — matches every scope limit in the spec. `tsc --noEmit`, `eslint`, and `npm run build` all pass clean; build output confirms `/api/ai/design` and `/api/ai/design/token` are registered as dynamic routes. Not exercised against a live Trigger.dev dev server in this session (no `trigger dev` process running) — route logic, migration, and typechecking are verified, but an actual end-to-end trigger → run → token flow is unconfirmed.

- `23-design-agent-logic.md` — `trigger/design-agent.ts` is now the full AI design agent, built on the exact server-side utilities the Liveblocks/Trigger.dev skills document for this: `mutateFlow` from `@liveblocks/react-flow/node` (a small helper — `client.mutateStorage` under the hood — exposing `addNode`/`updateNode`/`updateNodeData`/`removeNode`/`addEdge`/`updateEdgeData`/`removeEdge` against the same `flow`-keyed Storage document `useLiveblocksFlow` already reads/writes client-side, so no new state system was introduced) and `@liveblocks/node`'s `broadcastEvent`/`setPresence` (the latter is explicitly documented as "useful for scenarios like showing an AI agent's presence in a room" — an ephemeral, TTL-based presence update that requires no WebSocket connection, i.e. no persistent bot connection needed). Flow: `publishStatus("start", ...)` + `setAiPresence(thinking: true)` → open `mutateFlow` → `publishStatus("processing", ...)` → build a prompt from the current canvas graph (`flow.toJSON()`) plus the user's request → `generateObject` (from `ai`, using `@ai-sdk/google`'s `createGoogleGenerativeAI({ apiKey: process.env.GOOGLE_AI_API_KEY })`, model `gemini-2.5-flash`) against a zod `discriminatedUnion` schema of the seven required actions (`addNode`/`moveNode`/`resizeNode`/`updateNodeData`/`deleteNode`/`addEdge`/`deleteEdge`) → apply each action via `mutateFlow`'s helper methods (also nudging the AI's presence cursor to each new/moved node's position as it goes, so its cursor visibly "draws" across the canvas) → `publishStatus("complete", ...)` with a change count. `try/catch/finally`: on error, logs via `logger.error`, broadcasts an `"error"` status, and rethrows (so the run is correctly marked failed and Trigger.dev's existing retry config applies); `finally` always clears AI presence (`setAiPresence(false, null, ttl: 2)` — a 2-second TTL, the API's documented minimum, rather than an instant clear, since ephemeral presence has no "delete now" call). The zod schema itself is what enforces the spec's shape/color constraints: `shape: z.enum(CANVAS_SHAPES)` and `colorIndex: z.number().int().min(0).max(NODE_COLORS.length - 1)` make it structurally impossible for the model to emit a shape or color outside the existing `types/canvas.ts` palette; layout/spacing rules (no exact numbers exist in `context/ui-context.md` beyond node sizes) are instead enforced via explicit prompt instructions (spacing gaps, shape-to-purpose meaning, color-by-relationship guidance) baked into a `SYSTEM_PROMPT` constant. `liveblocks.config.ts`'s `RoomEvent` (previously `Record<string, never>`, unused since scaffolding) is now `{ type: "ai-status"; status: "start" | "processing" | "complete" | "error"; message: string }` — the "shared status feed" is this typed broadcast event, not a new pub/sub mechanism. New `components/editor/canvas/ai-status-banner.tsx` (`AiStatusBanner`) is a small top-center pill (`useEventListener` from `@liveblocks/react`) shown to every connected participant regardless of whether their AI sidebar is open — spinner for `start`/`processing`, checkmark for `complete`, alert icon for `error` (`text-success`/`text-error`/`text-ai-text` — theme tokens already mapped in `globals.css`, first use of them in a component), auto-dismissing 4s after a terminal status; rendered as a sibling of `<ReactFlow>` in `canvas.tsx`, next to the existing `LiveCursors`/`PresenceAvatars`. `components/editor/canvas/live-cursors.tsx` gained one line — appending `" · thinking…"` to a cursor's name badge when `other.presence.thinking` is true — which is enough to visualize the AI's presence, since the AI is just another `useOthers()` entry once `setPresence` is called (same typed `Presence.cursor`/`Presence.thinking` fields every human participant already uses, per `10-liveblocks-setup.md`/`19-presence-avatars-cursors.md` — no AI-specific presence path was added) and `PresenceAvatars` already renders any non-self `other` with initials-fallback (no avatar URL is set for the AI user, so it shows "GA"). Wiring the actual trigger: `components/editor/ai-sidebar.tsx`'s `ArchitectTab` (previously fully inert per `20-ai-sidebar-shell.md`'s scope limit) now takes a `projectId` prop (threaded from `workspace-shell.tsx`, which already had `projectId` in scope) and its `sendMessage` POSTs `{ prompt, roomId: projectId, projectId }` to the existing `/api/ai/design` route from `22-design-agent-api.md` after appending the local user-message bubble; on a non-OK response or network error it appends an inert `"assistant"`-role error bubble ("Ghost AI couldn't start that request...") — no other chat/assistant-reply logic was added, since all real progress/result feedback happens through the canvas (status banner, presence, live nodes/edges appearing), not through the chat transcript. `/api/ai/design/token` (from `22-design-agent-api.md`) is unused by this unit — the spec's real-time requirement is satisfied via Liveblocks (presence + broadcast + storage), not by subscribing to the Trigger.dev run itself, so no realtime-run UI was built against that token endpoint. Added `zod` (`^4.4.3`) as an explicit dependency (`npm install zod`) since the task file imports it directly for the action schema — it was previously only a transitive/hoisted dependency of `ai`/`@trigger.dev/*`, not something project code imported. No canvas architecture changes, no new state system, no bypassing of `mutateFlow`/`useLiveblocksFlow`, per scope limits. `tsc --noEmit`, `eslint` (scoped to the files this unit touched — the repo-wide `npm run lint` run picks up thousands of pre-existing warnings/errors from a stray, gitignored `.trigger/tmp/build-*` directory left behind by an earlier Trigger.dev CLI invocation; that directory predates this session, is not part of source, and isn't covered by `eslint.config.mjs`'s ignore list, but touching that ignore list wasn't part of this unit's scope), and `npm run build` all pass clean; build output confirms `/api/ai/design` and `/api/ai/design/token` are still registered. Not exercised against a live Trigger.dev dev server or a real Gemini call in this session (same limitation noted in `22-design-agent-api.md` — no `trigger dev` process was started) — the action schema, prompt construction, `mutateFlow` wiring, and status/presence broadcasting are verified by type-checking and code inspection only; an actual end-to-end prompt → Gemini → canvas-mutation flow is unconfirmed.

- `25-sidebar-chat-feed.md` — added a second, separate Liveblocks Feed for real-time room chat, reusing the exact Feeds mechanism `24-ai-presence-state.md` already established for `ai-status-feed` (`useFeeds`/`useFeedMessages`/`useCreateFeedMessage`), not a new realtime system. `types/tasks.ts` adds `AI_CHAT_FEED_ID` (`"ai-chat"`), `AI_CHAT_ROLES`/`AiChatRole` (`"user" | "assistant"` — only `"user"` is ever produced this unit, `"assistant"` reserved for a future AI-reply feature per the spec's "don't add AI-generated replies yet" scope limit), a zod `aiChatFeedMessageSchema` (`{ sender: string, role, content: string, timestamp: number }`), and `parseAiChatFeedMessage()`, mirroring `aiStatusFeedMessageSchema`'s validate-at-the-boundary pattern exactly. `liveblocks.config.ts`'s `FeedMessageData` (previously just `AiStatusFeedMessage`) is now a union `AiStatusFeedMessage | AiChatFeedMessage`, since Liveblocks Feeds' TS typing has one global `FeedMessageData` shared across every feed in the room — each feed's own ID is what keeps the two logically separate at the call site (`useFeedMessages(AI_STATUS_FEED_ID)` vs `useFeedMessages(AI_CHAT_FEED_ID)`), consistent with this spec's "keep it room-scoped" / "don't mix it with ai-status-feed" instructions. New `hooks/use-ai-chat-feed.ts` exports three hooks: `useAiChatMessages()` (wraps `useFeedMessages(AI_CHAT_FEED_ID)`, validates each message through `parseAiChatFeedMessage` and drops invalid ones, reverses Liveblocks' default newest-first order into oldest-first for a chat view — same loading/error → `null`/`[]` fallback pattern as `useLatestAiStatus`), `useEnsureAiChatFeed()` (client-side `useCreateFeed(AI_CHAT_FEED_ID)` on mount, `.catch()`-swallowed if it already exists — the client-side equivalent of `trigger/design-agent.ts`'s idempotent server-side `createFeed` call for `ai-status-feed`, needed here because chat messages originate from users in the browser, not a Trigger.dev task), and `useSendAiChatMessage()` (wraps `useCreateFeedMessage()`, stamps `timestamp: Date.now()` itself so callers can't spoof it, tracks a boolean `error` flag from the last send attempt, returns a `Promise<boolean>` so the caller knows whether to clear its input). `components/editor/ai-sidebar.tsx`'s `ArchitectTab` (previously local-only `useState<ChatMessage[]>`, from `20-ai-sidebar-shell.md`) now sources its message list entirely from `useAiChatMessages()` and sends via `useSendAiChatMessage()` — `useSelf()` (`@liveblocks/react`) supplies the sender's display name (`self.info.name`, the same Clerk-derived name set by `/api/liveblocks-auth`'s `identifyUser` call, so it's consistent with what avatars/cursors already show elsewhere in the room) rather than adding a second Clerk lookup. Each rendered message bubble now shows sender name + `toLocaleTimeString` timestamp above the content, per the spec's "show sender, timestamp, and message content"; a failed send (`sendError` from the hook) shows a small `text-error` line above the input instead of clearing it, and the input only clears after a confirmed successful send. Kept `trigger/design-agent.ts`'s pre-existing `/api/ai/design` POST (wired up in `23-design-agent-logic.md`, unrelated to and pre-dating this spec) firing on the same submit, since nothing in this spec asks for it to be removed — the spec's "don't trigger backend AI tasks" scope limit is read here as "the new chat-sending code itself shouldn't invoke a task," not as "delete the unrelated, already-working design-agent trigger this same input already had." Its failure no longer injects a fake local `"assistant"` chat bubble (that mechanism doesn't exist anymore now that messages come straight from the shared, validated feed) — status/errors for that flow already surface separately via `AiStatusBanner`/`ai-status-feed`, so the POST's `.catch()` is now a silent no-op with a comment explaining why. No changes to `ai-status-feed`, `AiStatusBanner`, or `trigger/design-agent.ts` — the two feeds remain fully independent, per scope limits. `tsc --noEmit`, `eslint` (scoped to touched files), and `npm run build` all pass clean; build output confirms every existing route still registers. Not verified in an actual browser (same `LIVEBLOCKS_SECRET_KEY`/no-browser-tool limitation noted in every Liveblocks-dependent unit since `11-base-canvas.md`) — the feed creation, message ordering/validation, sender/timestamp rendering, and send-error state are verified by type-checking and code inspection only.

- `26-ai-chat-functional.md` — wired the AI sidebar's submit flow to actually track a real Trigger.dev run instead of firing-and-forgetting the `/api/ai/design` POST from `23-design-agent-logic.md`/`25-sidebar-chat-feed.md`. `app/api/ai/design/route.ts` now also mints a run-scoped `auth.createPublicToken({ scopes: { read: { runs: [handle.id] } } })` (same call `app/api/ai/design/token/route.ts` from `22-design-agent-api.md` already made as a separate endpoint) and returns `{ runId, publicToken }` in one response, since this spec's step 1 explicitly reads both fields off the single `POST /api/ai/design` call rather than a follow-up token fetch; `.../design/token/route.ts` itself is untouched and now unused by the sidebar, left in place since removing it wasn't asked for. `components/editor/ai-sidebar.tsx`'s `ArchitectTab` gained real run-tracking state (`runId`/`publicToken`/`isSubmitting`) and `useRealtimeRun<typeof designAgent>(runId, { accessToken: publicToken, enabled: !!runId && !!publicToken, onComplete })` from `@trigger.dev/react-hooks` (per the `trigger-realtime-and-frontend` skill's documented pattern — a run-scoped token, `enabled` gated on both id+token existing so it never subscribes with a stale/empty pair); a local `TERMINAL_RUN_STATUSES` set (`COMPLETED`/`CANCELED`/`FAILED`/`CRASHED`/`INTERRUPTED`/`SYSTEM_FAILURE`/`EXPIRED`/`TIMED_OUT`) drives `isRunActive` off `run.status`. `onComplete` pushes one final `role: "assistant"` message to the `ai-chat` feed (`"Done! I've updated the canvas based on your request."` on `COMPLETED`, a generic failure message otherwise) via the existing `useSendAiChatMessage()` hook from `25-sidebar-chat-feed.md`, then resets `runId`/`publicToken` to `null` — this is the "when the run completes, push a final AI message + reset loading/run state" step; a caught fetch/non-OK response from the initial `POST /api/ai/design` also pushes an assistant error bubble into `ai-chat` (previously a silent `.catch()` no-op), per this spec's "show errors as messages in the ai-chat feed" instruction. `isGenerating` (`isSubmitting || isRunActive`) still drives the `Textarea`/send-button/starter-chip disabled state and the spinner-in-button swap exactly as before; the compact status strip (reading `useLatestAiStatus()` from `ai-status-feed`, unchanged from `24-ai-presence-state.md`) was moved from the top of the sidebar (rendered whenever any status-feed message said "start"/"processing", regardless of whether a run this sidebar's user is tracking is even active) down to directly above the input, and its visibility condition changed from a status-feed-only heuristic to the real `isRunActive` flag — matches this spec's literal "status strip above the input, shown only when a run is active" wording, and its layout note. No changes to `useLiveblocksFlow`/canvas mutation code, no manual node/edge syncing added, no new colors (reused `bg-accent-dim`/`text-brand`/`bg-brand` already used by this component pre-existing) — per scope limits. `tsc --noEmit`, `eslint` (scoped to touched files), and `npm run build` all pass clean; build output confirms `/api/ai/design` still registers. Not verified in an actual browser (same `LIVEBLOCKS_SECRET_KEY`/no-`trigger dev`-process limitation noted in every AI/canvas unit since `11-base-canvas.md`/`22-design-agent-api.md`) — the runId/publicToken round trip, `useRealtimeRun` status transitions, the disabled/spinner states, and the final chat message on completion are verified by type-checking and code inspection only.

- `27-spec-generation-flow.md` — backend-only flow for AI-powered spec generation, deliberately not wired to any UI yet (spec's own scope limits: no frontend logic, no spec editor, no persistence of the final spec). Shared Zod schemas for the new payload shape live in `types/tasks.ts` (`specChatMessageSchema`, `specCanvasNodeSchema`/`specCanvasEdgeSchema` — permissive `.passthrough()` object schemas rather than importing `CanvasNode`/`CanvasEdge` from `types/canvas.ts` directly, since those are `@xyflow/react` generic `Node`/`Edge` types, not Zod schemas, and the spec explicitly calls for Zod-validated input; `generateSpecPayloadSchema`, the full task payload) — put here rather than inline in `trigger/generate-spec.ts` so `app/api/ai/spec/route.ts` can import just the schemas without pulling the task module (and its `@ai-sdk/google`/`ai` imports) into the route's bundle, consistent with the SDK's "type-only import the task itself" rule. New `trigger/generate-spec.ts` exports `generateSpec`, a `schemaTask` (`id: "generate-spec"`, validated by `generateSpecPayloadSchema`) that calls `generateText` (from `ai`, same `@ai-sdk/google`/`gemini-flash-latest` pairing `trigger/design-agent.ts` already uses) with a system prompt describing a Markdown spec (Overview/Architecture/Data Flow/Key Decisions sections) built from a plain-JSON summary of the canvas nodes/edges/chat history, and returns `{ content: text }` as plain Markdown task output — no `mutateFlow`, no canvas writes, no Blob/Prisma persistence, per scope limits. Realtime tracking uses `metadata.set("status", ...)` (`"start"` → `"generating"` → `"complete"`/`"error"`) rather than `design-agent.ts`'s Liveblocks-feed broadcast — the spec's own wording ("updates run metadata/status for realtime tracking") matches the SDK's run-metadata mechanism (readable via `useRealtimeRun`), not a second Liveblocks feed, and this task has no canvas-mutation reason to touch Liveblocks at all. New `app/api/ai/spec/route.ts`: `POST` auth-checks via `getCurrentIdentity`, validates the body against a request schema built from the same shared node/edge/chat schemas, resolves the `Project` from the posted `roomId` (never a client-supplied `projectId` — matches every prior project route's ownership pattern) via `hasProjectAccess`, triggers `generate-spec` with a server-resolved `projectId`, creates a `TaskRun` row (same shared table `22-design-agent-api.md` added — no `type` column distinguishing design vs. spec runs, since nothing yet needs to query by kind), and returns only `{ runId }` (unlike `23-design-agent-logic.md`'s later `/api/ai/design` which also returns a `publicToken` in the same call — this spec describes the token as a separate follow-up endpoint, so it was kept that way rather than merging them). New `app/api/ai/spec/token/route.ts` mirrors `/api/ai/design/token`'s ownership check (`taskRun.userId !== identity.userId` → `403`) but adds `expirationTime: "1h"` to `auth.createPublicToken`, per this spec's explicit 1-hour requirement (the design-agent token endpoint has no expiry set). `tsc --noEmit`, `eslint` (scoped to touched files), and `npm run build` all pass clean; build output confirms `/api/ai/spec` and `/api/ai/spec/token` are registered as dynamic routes. Not exercised against a live Trigger.dev dev server or a real Gemini call (same limitation noted for every AI unit since `22-design-agent-api.md`) — route logic, task schema validation, and the metadata status transitions are verified by type-checking and code inspection only.

- `28-spec-persistence-download.md` — new `prisma/models/project-spec.prisma` adds `ProjectSpec` (`id`, `projectId` relation to `Project` with `onDelete: Cascade`, `filePath`, `createdAt`, `@@index([projectId, createdAt])`) — metadata only, matching `05-prisma.md`/`21-canvas-autosave.md`'s existing metadata-in-Postgres/content-in-Blob split; `Project` gained a `specs ProjectSpec[]` back-relation. Migration `prisma/migrations/20260729040759_add_project_spec` applied cleanly via `prisma migrate dev` (no `P1017` recurrence this session, unlike the `Session Notes` quirk from `05-prisma.md`) and the client regenerated. `trigger/generate-spec.ts` (from `27-spec-generation-flow.md`, previously returned `{ content: text }` with no persistence, per that unit's own scope limits) now persists after a successful `generateText` call: creates a `ProjectSpec` row first (empty `filePath` placeholder) to obtain a real `id`, uploads the Markdown to Vercel Blob at `specs/{projectId}/{specId}.md` (`access: "private"`, `addRandomSuffix: false`, `allowOverwrite: true` — same flags `21-canvas-autosave.md`'s canvas route already established for this store) using that id, then updates the row's `filePath` to the returned blob URL; the task's return value gained `specId` alongside the existing `content` field. New `app/api/projects/[projectId]/specs/[specId]/download/route.ts`: `GET` auth-checks via `getCurrentIdentity`/`hasProjectAccess` (same helper every project route uses), 404s if the project doesn't exist, 403s if the caller lacks access, looks up the `ProjectSpec` by `specId` and 404s if it's missing or its `projectId` doesn't match the route's `projectId` (prevents fetching a real spec ID under the wrong project), reads the blob via `@vercel/blob`'s `get(filePath, { access: "private" })` (same private-store pattern as the canvas route's post-`21-canvas-autosave.md` follow-up fix), and returns the Markdown as a `text/markdown` attachment with a `Content-Disposition: attachment; filename="spec-{specId}.md"` header. No frontend/UI changes, no spec content stored in Prisma (only the Blob URL), no Blob URLs returned to the client without the access checks running first, no changes to canvas persistence — matches every scope limit in the spec. `tsc --noEmit` (required a `prisma generate` first — the new `ProjectSpec` model didn't exist on the generated `PrismaClient` type until regenerated), `eslint`, and `npm run build` all pass clean; build output confirms `/api/projects/[projectId]/specs/[specId]/download` is registered as a dynamic route. Not exercised against a live Trigger.dev dev server or a real Gemini call (same limitation noted for every AI unit since `22-design-agent-api.md`) — the create-then-upload-then-update persistence sequence and the download route's access/not-found branches are verified by type-checking and code inspection only.

- `29-spec-ui-integration.md` — wires the previously backend-only `ProjectSpec` flow (`27-spec-generation-flow.md`/`28-spec-persistence-download.md`) into the existing AI sidebar's Specs tab (`components/editor/ai-sidebar.tsx`), which until now only rendered a static hardcoded placeholder card. Two thin new backend routes were required since neither existed yet — the spec's "existing ProjectSpec API" / "existing endpoint" wording assumed them but only the download route (`28-spec-persistence-download.md`) had been built: `GET app/api/projects/[projectId]/specs/route.ts` lists a project's specs (same `getCurrentIdentity`/`hasProjectAccess` auth pattern as every other project route) and returns metadata only (`id`, `createdAt`, and a derived `filename: spec-{id}.md` — `ProjectSpec` has no filename column, so this matches the existing download route's `Content-Disposition` naming); `GET app/api/projects/[projectId]/specs/[specId]/route.ts` mirrors the download route's auth/lookup/Blob-`get` logic but returns the content as JSON (`{ id, createdAt, filename, content }`) instead of a file attachment, so the client never touches a Blob URL directly, per the spec's scope limit. `SpecsTab` now fetches the list on mount (plain `fetch` + `useState`/`useEffect`, matching `ArchitectTab`'s existing fetch pattern rather than introducing a data-fetching library), renders it in a `ScrollArea`-scrolled compact list, and clicking an item opens a `Dialog`-based preview modal (`SpecPreviewDialog`) that fetches content from the new `[specId]` route and renders it with `react-markdown` (newly installed — no Markdown renderer existed in the repo) inside a `.markdown-body` class added to `globals.css` (styled with the existing CSS custom properties/tokens from `ui-context.md`, since no Tailwind Typography plugin is installed and adding one would've been a heavier dependency than a dozen scoped rules). Download is wired at both the list-item level and inside the modal via a shared `downloadSpec()` helper that does `window.open` on the existing download route — browser handles the file save, no client-side Blob handling. `SpecPreviewDialog` is remounted via a `key={selectedSpecId ?? "none"}` on the parent rather than resetting its `spec` state inside a `useEffect` early-return branch — the latter tripped this repo's `react-hooks/set-state-in-effect` lint rule (synchronous `setState` in an effect body outside an async callback), and remounting on key change is the idiomatic escape hatch for "reset all state when this prop changes" per that rule's own guidance. No backend generation/persistence logic touched, no Blob URLs returned to the client, no spec content kept in state beyond the currently-open modal, sidebar/tab layout unchanged — matches every scope limit in the spec. `tsc --noEmit`, `eslint` (scoped to touched files), and `npm run build` all pass clean; build output confirms both new routes are registered as dynamic routes alongside the existing download route. Not exercised in a live browser session (same live-collaboration-room limitation blocking end-to-end verification since `11-base-canvas.md` — see `Next Up`) — verified by type-checking, lint, and build only.

## Next Up

- Fill in a real `LIVEBLOCKS_SECRET_KEY` in `.env.local`, then verify the canvas actually connects/syncs in a real browser session (not yet done — see `11-base-canvas.md` notes). This also blocks verifying `12-shape-panel.md`'s drag-and-drop, `13-node-shape.md`'s shape rendering/ghost preview, `14-node-editing.md`'s resize/label-editing interactions, `15-node-color-toolbar.md`'s toolbar, `16-edge-behavior.md`'s handles/edge/label interactions, `18-starter-templates.md`'s import flow, `19-presence-avatars-cursors.md`'s avatar stack/live cursors, `23-design-agent-logic.md`'s full prompt → Gemini → canvas-mutation → status/presence flow, and `24-ai-presence-state.md`'s feed-based sidebar status indicator/cursor spinner — all of which require a live collaboration room (and, for `23-design-agent-logic.md`, a running `trigger dev` process plus a real Gemini API call) to see end-to-end. `20-ai-sidebar-shell.md`'s slide-in/tabs/chat UI is local-only state and can be verified without a Liveblocks connection.

## Open Questions

- Add unresolved product or implementation questions here.

## Architecture Decisions

- `prisma.config.ts` now also reads `shadowDatabaseUrl` from `SHADOW_DATABASE_URL` (was previously unset). Needed to get `prisma migrate dev`/shadow-DB diffing working at all against the local `prisma dev` server.
- Home shell (`/editor`) and the workspace route (`/editor/[roomId]`) intentionally use different layout components since Next.js layouts apply to all nested segments — the home route was moved into a route group `app/editor/(home)/` so it doesn't wrap `/editor/[roomId]`.

## Session Notes

- Local Prisma Postgres dev server: `.env`'s `DATABASE_URL` points at `prisma+postgres://localhost:51213/...`, which requires `npx prisma dev` to be running (started it in the background this session — PID may not survive session end, restart with `npx prisma dev` if `DATABASE_URL` can't connect).
- `npx prisma migrate dev` itself reliably fails against this local dev server with `P1017 Server has closed the connection` (both against the local proxy URL and the raw `postgres://localhost:51214` URL it prints), even though plain `pg` queries and `prisma db push`/`prisma migrate diff`/`prisma migrate deploy` all work fine against the same URLs. Worked around it by generating the SQL with `prisma migrate diff --from-empty --to-schema prisma/ --script`, hand-placing it in a timestamped `prisma/migrations/<ts>_init_project_models/migration.sql`, and applying with `prisma migrate deploy` (which records it normally in `_prisma_migrations`). `prisma migrate status` confirms the schema is up to date. Future migrations should try `migrate dev` first — this may be an environment/version-specific quirk — and fall back to the diff+deploy approach if it recurs.
